# 首版验证记录

## 2026-09-19 图片总榜与地图子页更新

- 生产构建和类型检查通过，输出独立的 `rank/index.html` 与 `rank/guide/index.html`。
- 8 项数据/业务测试通过，新增固定城市色、坐标范围/WGS84、导航经纬度顺序、视频链接/片段时间/本地视频路径校验。
- 7 个浏览器场景已通过（完整导出测试的状态选择器修正后单独复跑通过）：原有榜单/PNG 流程、新子页直达与刷新、图片模式与城市色、视频文字与时间、完整 HTML 导出、地图与目录联动、底图错误重试。
- 新子页及含测试条目的页面在 375/768/1440px 检查无横向溢出；实际查看桌面和手机截图。
- HTML 文件已实际下载并检查嵌入图片、完整文字、视频出处、筛选范围、全榜切换及隐藏条目排除；介绍中的 HTML 字符按文字转义。
- 地图交互测试使用模拟瓦片和虚构地点，未在公开数据中写入这些内容；已验证地图失败降级和重试。未声称实际手机上已唤起高德 App，也未播放尚未提供的真实视频。
- 正式数据仍为两个空数组，等待用户提供店铺图片、位置及视频素材。

## 2026-09-18 初版

验证日期：2026-09-18。正式内容为 `places.json = []`、`updates.json = []`，没有公开虚构条目。

已实际执行：

- Node 24 LTS；安装依赖并生成锁文件；生产构建、Vue/TypeScript 类型检查通过。
- 5 项自动化数据/业务测试通过：空榜单、真实日期、重复 ID、非法档位、缺图、路径约束、更新引用、升降档、搜索筛选排序、隐藏条目与更新记录。
- 3 项 Edge 浏览器测试通过：空榜单完整流程；28 条虚构公开条目与 1 条隐藏条目的交互；图片导出流程。
- 375px 手机、768px 平板、1440px 桌面实际截图，无横向溢出。检查五档顺序、空档、缺图与长中文名称布局。
- 检查组合筛选、URL 恢复、原档内排名、重置、不存在 ID、隐藏 ID、详情直达/刷新/返回。
- 两种 1200px 宽 PNG 都已实际保存；空榜单、含图片/中文/多行评价的榜单、当前筛选结果均可导出。28 条短评测试自动拆成 3 页，没有截断卡片。实际打开导出图检查内容与分页。
- 检查图片失败提示、修复后重试、重复导出、Escape 关闭与键盘焦点返回。
- 原个人主页、博客、学习经历、项目经历及原有 CSS/头像不修改；发布脚本同时保留原站文件和榜单构建产物。

本地证据在 `rank/test-results/evidence/`，不上传到网站。测试素材在 `rank/.fixture-app/`，被 Git 忽略，不是正式内容。

线上部署结果以仓库 Actions 的实际运行结果与线上访问为准。自定义域名暂未购买，使用默认 GitHub Pages 地址。

未声称覆盖：Safari/Firefox 的实机测试、真实手机的分享面板；第一版不包含可视化内容编辑器、拖动改档和在线账号发布。
